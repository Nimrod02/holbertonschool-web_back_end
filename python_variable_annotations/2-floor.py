#!/usr/bin/env python3
"""module 2 - floor
"""


def floor(n: float) -> int:
    """a type-annotated function floor
    which takes a float n as argument
    and returns the floor of the float.

    Args:
        n (float): n wich is a float

    Returns:
        int: n but with a int
    """
    return int(n)
